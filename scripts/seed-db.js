async function seedDatabase() {
  console.log('Seeding is now handled through the Flask backend and Neon migrations.');
  console.log('Use the backend API or the migration scripts in cpp-backend instead of this Node script.');
}

seedDatabase();
